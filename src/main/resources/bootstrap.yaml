spring:
  application:
    name: autorizacion-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/tingeso
    username: root
    password: 4892
  jpa:
    hibernate:
      ddl-auto: update